# quantum_flow development environment
use flake .#default

# Set database URL for testing
export DATABASE_URL="postgresql://postgres:postgres@localhost:5432/quantum_flow"

# Allow direnv to load this environment
# Run: direnv allow
# Then: cd packages/quantum_flow  # environment loads automatically