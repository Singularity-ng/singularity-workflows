# ex_pgflow development environment
use flake .#default

# Set database URL for testing
export DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ex_pgflow"

# Allow direnv to load this environment
# Run: direnv allow
# Then: cd packages/ex_pgflow  # environment loads automatically